# Plan: unify await callee protocol to 1 return

## Goal

Collapse `f` and `__atmos` from `(ok, ret)` two-return to a single `r`,
matching the `until`/`while` predicate rule.

## Unified rule (callee returns one `r`)

- falsy      -> reject (keep waiting)
- `true`     -> accept, result = the event
- other      -> accept, result = `r`

Trade-off: cannot accept with a literal `false`/`nil` result (`0`/`""`
stay truthy in Lua, so they still work).
Behavior change: a plain-boolean `f`/`__atmos` now returns the event
(was `nil`).

## Status

- [x] run.lua: `f` (~584) -> single `r`, return `(r~=true) and r or emt`
- [x] run.lua: `__atmos` awt-side (~556) -> same collapse
- [x] run.lua: `__atmos` emt-side (~599) -> same collapse
- [x] api.md: `f:` / `until` / `__atmos` returns cells -> `e / res`

Untouched: `while` (returns `e`), `catch` (~225), `par_or` plumbing (508).

## Tests affected (2-return form folded to single-return)

Two existing tests used the old `(gate, value)` `f` form. Folded to
`return gate and value`:

- [x] tst/par.lua:353  `... and e[1]==10 and e[1]`
- [x] tst/task.lua:379 `... and v>10 and v`

- [x] re-run full suite (user) -> all tests pass

PLAN COMPLETE.
